view { }

.title {
  font-size: 16px;
  font-weight: bold;
  margin-left: 12px;
}

.library {
  width: 50%;
}

.avatar {
  width: 50px;
  height: 50px;
  margin-right: 20px;
  margin-top: 10px;
}

.products {
  margin-top: 12px;
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
.container {
  flex: 1;
}